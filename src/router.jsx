import { BrowserRouter as Router, Route, Routes } from 'react-router-dom';


const AppRouter = () => {
    return (
      <Router>
        <Routes>
          {/* <Route path="/" element={<Home />} /> 이런식으로 라우터 연결*/}
        </Routes>
      </Router>
    );
  }
  
export default AppRouter;